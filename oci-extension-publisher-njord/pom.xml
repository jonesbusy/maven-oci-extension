<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>cloud.jonesbusy</groupId>
        <artifactId>oci-extension-parent</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <artifactId>oci-extension-publisher-njord</artifactId>
    <packaging>jar</packaging>

    <description>
        njord ArtifactStorePublisher/ArtifactStorePublisherFactory implementation that pushes a staged
        njord ArtifactStore to an OCI registry via oras-java. Declared as a sibling core extension next to
        njord's own extension3 in .mvn/extensions.xml -- no njord source changes required.

        NOTE: the njord dependency coordinates/version below are a placeholder pending the Phase 1 spike
        (see plan) that confirms njord's publisher SPI is consumable as an external library dependency.
    </description>

    <dependencies>
        <dependency>
            <groupId>cloud.jonesbusy</groupId>
            <artifactId>oci-extension-common</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>land.oras</groupId>
            <artifactId>oras-java-sdk</artifactId>
        </dependency>

        <dependency>
            <groupId>javax.inject</groupId>
            <artifactId>javax.inject</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>land.oras</groupId>
            <artifactId>oras-java-sdk</artifactId>
            <classifier>tests</classifier>
            <type>test-jar</type>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.eclipse.sisu</groupId>
                <artifactId>sisu-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>
